sudo: false
language: node_js
node_js:
  - "node"
install:
- npm install -g webpack
before_script: npm run build-tests
script: npm run travis

env:
  - CXX=g++-4.8

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
  firefox: latest

