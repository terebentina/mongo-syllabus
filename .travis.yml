sudo: false
language: node_js
node_js:
  - "node"
script: npm run travis

env:
  - CXX=g++-4.8

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
  firefox: 44.0

